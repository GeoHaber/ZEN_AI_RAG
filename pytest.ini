[pytest]
testpaths = tests
norecursedirs = _archive .git dist build __pycache__
asyncio_mode = auto
markers =
    integration: marks tests that require external resources (datasets path, embedding model, optional LLM)
